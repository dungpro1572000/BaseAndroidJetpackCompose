package com.dungz.data.remote.dto

/**
 * Base DTO interface for network models.
 * All DTOs should implement this interface.
 */
interface BaseDto {
    // TODO: Add common fields if needed (e.g., id, timestamp, etc.)
}
